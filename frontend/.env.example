# Frontend (React / CRA)
# Copy to frontend/.env for local dev.
# For production builds on a host, set these as build-time env vars.

# Base URL for the backend API (NO trailing slash)
# Example: https://api.your-domain.example
REACT_APP_API_URL=https://your-backend.example
