{
  "name": "smart-scheduler-root",
  "private": true,
  "engines": {
    "node": "18.20.4",
    "npm": "10.x"
  },
  "dependencies": {
    "yaml": "2.8.2"
  },
  "scripts": {
    "build": "cd frontend && npm ci && npm run build && cd .. && node scripts/copy-frontend-build.mjs"
  }
}